# Immediate next steps

1. Open and review the v0.1 pull request.
2. Let GitHub Actions run the TypeScript and Docker build.
3. Fix any compatibility errors found by CI.
4. Merge v0.1 into `main`.
5. Clone `main` on the Hetzner server and create `.env`.
6. Start the container and configure Nginx.
7. Connect the first Google account.
8. Add the MCP endpoint to ChatGPT Work.
